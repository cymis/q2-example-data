# Usage Example

Download the Gut-to-Soil tutorial sample metadata as an `ImmutableMetadata`
artifact:

```bash
qiime example-data get-gut-to-soil-metadata \
  --o-metadata gut-to-soil-metadata.qza
```

Environment hint:
```bash
conda activate qiime2-tiny-2026.1
```
